{
  "name": "chatbot-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docker-compose up -d",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.0.0"
}
